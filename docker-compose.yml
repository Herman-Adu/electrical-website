services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SKIP_ENV_VALIDATION: "true"
    image: electrical-website:local
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
    restart: unless-stopped
