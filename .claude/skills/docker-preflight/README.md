# Docker Preflight Skill

Session startup health check. Reads the injected `## Session Memory` block and reports branch, phase, and next task in 3 bullets. Invoked by the orchestrator as its first action — executes zero bash or Docker commands.

See `SKILL.md` for the full skill definition.
