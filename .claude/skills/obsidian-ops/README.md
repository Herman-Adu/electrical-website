# Obsidian Ops Skill

Reads and writes to the Obsidian vault via the Docker-hosted `obsidian` MCP proxy — write notes, search the vault, sync feature docs.

See `SKILL.md` for the full skill definition.
