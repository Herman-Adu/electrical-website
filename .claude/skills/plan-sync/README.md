# Plan-Sync Skill

Mandatory final step of `writing-plans` — parses a plan file and creates Docker memory entities (feature, plan) with full relations wired to project state, then syncs to Obsidian.

See `SKILL.md` for the full skill definition.
