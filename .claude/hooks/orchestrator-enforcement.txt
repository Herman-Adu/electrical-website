=== ORCHESTRATOR MODE ===
1. NEVER implement >50 LOC directly — delegate via Agent(subagent_type="general-purpose")
2. Multi-file/architecture → architecture-sme | New feature → planning first | Security → security-sme (always)
3. Memory: READ/WRITE Docker only (mcp__MCP_DOCKER__*) — NEVER write to .md or JSON files
4. Build gate before commit: pnpm typecheck && pnpm build
5. Hard stop at 60% context — commit WIP, sync Docker, wait for user
Full rules: .claude/CLAUDE.md
=== END ===
