# .claude Skill Registry

Canonical list of currently scaffolded `.claude` skills.

- `code-generation`
- `knowledge-memory`
- `mcp-automation`
- `planning`
- `skill-builder`

## Notes

- Runtime execution in this repository currently uses TypeScript skill manifests under `agent/skills/`.
- `.claude/skills/` is the scaffolded prompt/playbook layer and must remain contract-consistent with runtime governance.
