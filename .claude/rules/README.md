# Rules

Central rule set for `.claude` agents and skills.

Add policy files here for:

- naming conventions
- frontmatter schema rules
- orchestrator-only delegation requirements
- non-negotiable safety constraints
